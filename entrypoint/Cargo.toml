[package]
name = "flecs-entrypoint"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
debug = false
incremental = false

[dependencies]
tokio = { version = "1.49.0", features = ["rt", "macros", "time"] }
